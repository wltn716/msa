spring:
  profiles: default

security:
  oauth2:
    client:
      client-id: auth_id
      client-secret: auth_secret
      access-token-uri: http://loaclhost:8095/oauth/token
      user-authorization-uri: http://loaclhost:8095/oauth/authorize
    resource: 
      jwt:
        key-value: helloworld

---
spring:
  profiles: dev

security:
  oauth2:
    client:
      client-id: auth_id
      client-secret: auth_secret
      access-token-uri: http://${auth.host}:8095/oauth/token
      user-authorization-uri: http://${auth.host}:8095/oauth/authorize
      resourceURI: http://${api.gateway.host}:8090/api/**
    resource: 
      jwt:
        key-value: helloworld


        